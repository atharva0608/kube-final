# Infrastructure Onboarding

## Overview
`infrastructure/onboarding` is the documentation entry for the `onboarding` module within the BalanceKube repository.
Customer onboarding, registration, and cluster discovery.
This folder is part of the `infrastructure` domain and provides focused behavior for `Onboarding`.

## Current folder responsibilities
- Owns the module-level responsibilities for `onboarding` in the `infrastructure` domain.
- Implements the primary behavior and contracts for the `Onboarding` feature area.
- Supports the broader `Infrastructure` workflow and integrates with sibling modules in the same domain.

- This leaf module is one part of the `infrastructure` domain within `infrastructure`. Related sibling modules include `aws`, `cluster_inventory`, `drift_detection`, `eligibility_engine`, `execution`, `kubernetes`, `metrics_collection`, `monitoring`, `pricing_collection`, `recommendations`, `resource_analysis`, `rollback`, `snapshot_assembly`, `spot_risk_collection`, `workload_classification`, `workload_review`.

## Subfolders
- This module has no further nested subfolders.

## Related documentation
- `balancekube.md` for the overall BalanceKube architecture and domain relationships.
- `infrastructure/infrastructure.md` for the root of the `infrastructure` domain documentation.
- `infrastructure/onboarding/onboarding.md` for the parent domain documentation, if available.

## Notes
- Use this document to describe the folder purpose, submodule summaries, and cross-domain interactions.
